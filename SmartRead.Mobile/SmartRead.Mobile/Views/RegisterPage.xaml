<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Shell.FlyoutBehavior="Disabled" 
             x:Class="SmartRead.Mobile.Views.RegisterPage">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Label Text="Ime"/>
            <Entry Text="{Binding Ime}">
            </Entry>
            <Label Text="Prezime"/>
            <Entry Text="{Binding Prezime}">
            </Entry>
            <Label Text="Mail"/>
            <Entry Text="{Binding Mail}">
            </Entry>
            <Label Text="Username"/>
            <Entry Text="{Binding Username}">
            </Entry>
            <Label Text="Datum Rođenja"/>
            <DatePicker Date="{Binding DatumRodjenja}">
            </DatePicker>
            <Label Text="Password"/>
            <Entry Text="{Binding Password}" IsPassword="True">
            </Entry>
            <Label Text="Potvrdi Password"/>
            <Entry Text="{Binding PotvrdiPassword}" IsPassword="True">
            </Entry>
            <Button VerticalOptions="Center" Text="Register" Command="{Binding RegisterCommand}"/>
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>